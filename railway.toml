[build]
builder = "dockerfile"

[deploy]
startCommand = "cd backend && exec uvicorn server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
